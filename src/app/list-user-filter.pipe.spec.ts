import { ListUserFilterPipe } from './list-user-filter.pipe';

describe('ListUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ListUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
